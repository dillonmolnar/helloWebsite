# AWS Amplify: static site from repo root (no build tools).
version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static files only — nothing to build"
  artifacts:
    baseDirectory: .
    files:
      - "**/*"
